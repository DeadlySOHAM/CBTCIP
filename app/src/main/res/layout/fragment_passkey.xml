
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.soham.todo.fragments.PasskeyFragment">

    <data>
        <variable
            name="fragmentLogin"
            type="com.soham.todo.fragments.PasskeyFragment" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        style="@style/main_background">

        <EditText
            android:id="@+id/passkey_input"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_margin="24dp"
            android:hint="@string/enter_passkey_hint"
            android:inputType="numberPassword"
            android:padding="16dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <Button
            android:id="@+id/login_button"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="24dp"
            android:clickable="true"
            android:focusable="true"
            android:onClick="@{()->fragmentLogin.Login()}"
            android:text="@string/ok_button_text"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/passkey_input" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>